
    for(let i = 2; i < process.argv.length; ++i) { // initialization of a 2 to retrieve only user arguments
        console.log(`${process.argv[i]}`); // the loop causes the arguments to display inline
    }
